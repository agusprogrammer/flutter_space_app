import 'package:flutter/material.dart';

class Spacecraft extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child:  Text('Spacecraft'),
    );
  }

}